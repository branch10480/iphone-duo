# AGENTS.md

DuoLab は iOS のヒンジ・2面レイアウトを検証するサンプルアプリ。
このファイルを入口にし、作業に必要な資料だけ読む。説明・観測履歴は docs に置く。

## 最初に読む

| 作業 | 読む場所 |
|---|---|
| ビルド・起動・MCP・詰まったとき | [開発手順](docs/development.md) |
| API の意味・レイアウト方針・検証項目 | [対応リサーチ](docs/iphone-duo-support.md)（§4 API、§6 検証項目） |
| 過去の実測・残作業 | [引き継ぎ記録](docs/handoff-next-session.md) |
| SDK の宣言を確かめる | [保存済み SDK ヘッダ](artifacts/ios27.1-sdk/) |

## 作業中の判断

- ビルド前に開発手順を読む。プロジェクト設定の正は `project.yml`、ソースは `DuoLab/`。
- プロジェクト生成は `xcg_generate_project`、ビルド・シミュレータ・UI操作は `xcb_*` MCP を使う。tool名・引数は公開中のschemaで確認し、無いtoolを推測して呼ばない。
- `tool/gen-project.py` は過去の代替生成器。現在の生成経路では使わず、pbxproj を手書きで直さない。MCPが使えなければ開発手順の診断へ進む。
- 履歴に書かれた「起動中」「ビルド済み」「push済み」や端末IDを現在の状態とみなさない。該当MCPとGitで確認する。
- 文字列・識別子の発見、実行経路の確認、操作成功を区別する。検索の一致なしだけでAPIの不存在や権限拒否と断定しない。
- 調査目的・試した経路・結果を短く整理する。新しい根拠が増えないときは、手段を切り替えるか必要なユーザー操作を具体的に伝える。同じ内部API探索を続けない。
- 過去のAXプローブやsandbox回避策を、そのまま実行許可とみなさない。公開済みtoolと現在の権限の範囲で進める。
- 一時ファイルは用途と保存先を把握し、必要な範囲だけ生成する。既存の `.tmpx/` は過去の調査資料なので一括削除しない。自分が今回作った使い捨てだけを後片付けの対象にする。
- UIを変えたら実際の表示を確認する。利用できれば RenderPreview を使い、無ければMCPのスクリーンショットで確認し、その方法を報告する。

## 完了時

- 変更に応じたビルド・起動・対象画面の確認結果と、未確認の範囲を報告する。
- 再利用する手順は `docs/development.md`、APIの根拠は `docs/iphone-duo-support.md`、今回の観測と残作業は `docs/handoff-next-session.md` に反映する。
- この索引には毎回の判断規則とリンクだけを残す。観測値・長いログ・端末IDは増やさない。
